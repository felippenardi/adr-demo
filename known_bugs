gulpfile.js
- copy-bootstrap-less-files-except-variables
  - bootstrap variables.less file is not being ignored
    - could be working.  not sure.

tests
- my tests are using lodash attached to the window as opposed to injecting the service

categoryDirective
- hard coding caseId when calling notesModel.list(1)

noteDirective
- linkMode is not working because vm.linkMode does not update when the service does
